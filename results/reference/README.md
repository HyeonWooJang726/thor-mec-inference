# Canonical reference

| Navigation entry | Existing physical target | Tracked target evidence |
|---|---|---|
| `local_canonical_c2` | `results/local_canonical_c2/` | 66 existing tracked files, including `CHECKPOINT.md`. |

`local_canonical_c2` is a relative symlink to `../local_canonical_c2`. This remains
a current canonical Local reference, not a deprecated result. Its frozen code,
hash pins, and physical path are unchanged. Tracked reports/source are available
in Git; ignored per-run raw data still requires the local artifact store.
