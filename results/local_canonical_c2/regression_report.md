58 CPU regression tests PASS (27 existing Local, 24 new/copy termination and canonical tests, 7 prior EOS tests); zero failures. Existing test temporary-root default was rebound only in memory to isolate a fixture from populated protected artifacts. Full EOS missing and NULL failure remain rejected; low-load idle capacity is valid; alias and same-context overlap remain rejected. No old file modified.
